# Admin dashboard password. Change this before deploying.
# If unset, the app falls back to "admin123" at runtime (documented in README).
ADMIN_PASSWORD=admin123

# Optional: server port (defaults to 3000)
# PORT=3000